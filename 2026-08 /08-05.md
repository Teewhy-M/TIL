# 📅 2026-08-05 (수) 학습 일지

## 📝 오늘 배운 내용 (Summary)
> 오늘 새로 알게 된 핵심 개념을 1~2줄로 요약합니다.
* Git 명령어 흐름 (`add` -> `commit` -> `push`) 및 `origin` 별명의 개념 정리
* SSH 키 등록을 통한 GitHub 인증 설정

---

## 💡 주요 개념 및 실습
### 1. Git Push와 Origin
* `origin`은 복잡한 GitHub Remote URL에 붙여둔 **별명(Alias)**이다.
* `git clone`을 실행하면 Git이 자동으로 원격 주소를 `origin`으로 할당한다.

### 2. Fetch와 Push의 차이
* `(fetch)`: 원격 저장소에서 변경 이력을 가져오는 주소 (`git pull` 등)
* `(push)`: 내 로컬 커밋을 원격 저장소로 업로드하는 주소 (`git push`)

---

## 🔍 문제 해결 및 팁 (Troubleshooting & Tips)
* **문제:** `git push` 실행 시 upstream 연결 에러 발생
* **해결:** 처음에 `git push -u origin <브랜치명>` 옵션을 사용하여 로컬 브랜치와 원격 브랜치를 매핑함

---

## 🎯 내일 할 일 / 느낀 점 (To-Do & Retrospective)
- [ ] Python 클래스 및 객체지향 개념 정리하기
- [ ] Git Branch 생성 및 Merge 실습
- **느낀 점:** 개념을 단순히 외우는 것보다 명령어 내부 동작 흐름을 도식화해서 이해하니 훨씬 명확해졌다.
